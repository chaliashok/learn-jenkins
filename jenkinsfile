pipeline {
    agent { node { label 'workplace' } }
    stages {
        stage('workplace') {
            steps {
                echo "hello world"
                sh 'env'
            }
        }
    }
}